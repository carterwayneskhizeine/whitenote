// Worker is now integrated into the Next.js app via instrumentation.ts.
// Run `pnpm dev` — no separate worker process needed.
console.log("Worker now runs inside the Next.js process (via instrumentation.ts).")
console.log("Start the app with: pnpm dev")
